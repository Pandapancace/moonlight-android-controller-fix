name: Android CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build nonRoot_game Release APK
        run: ./gradlew assembleNonRoot_gameRelease -x lintVitalNonRoot_gameRelease

      - name: Upload APKs as artifact
        uses: actions/upload-artifact@v4
        with:
          name: Artemis-nonRoot-game-release
          path: app/build/outputs/apk/nonRoot_game/release/*.apk
          retention-days: 14
